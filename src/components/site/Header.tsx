import { useEffect, useState } from "react";
import { Link, useRouterState } from "@tanstack/react-router";
import { Menu, X } from "lucide-react";
import { cn } from "@/lib/utils";
import { NAV_LINKS, TAGLINE } from "@/lib/brand";
import { WhatsAppButton } from "@/components/site/Actions";
function Wordmark({ onClick }: { onClick?: () => void }) { return <Link to="/" onClick={onClick} aria-label="Megha Parekh — TRIKAAL, home" className="group flex flex-col leading-none"><span className="wordmark text-lg leading-tight sm:text-xl">Megha Parekh</span><span className="trikaal-mark mt-1 transition-colors group-hover:text-oxblood">Trikaal</span></Link>; }
export function Header() { const [scrolled,setScrolled]=useState(false); const [open,setOpen]=useState(false); const pathname=useRouterState({select:(s)=>s.location.pathname}); useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>24);onScroll();window.addEventListener("scroll",onScroll,{passive:true});return()=>window.removeEventListener("scroll",onScroll);},[]); useEffect(()=>{setOpen(false);},[pathname]); useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow="";};},[open]); return <header className={cn("sticky top-0 z-50 transition-all duration-300",scrolled?"border-b border-border bg-background/92 backdrop-blur-md":"border-b border-transparent bg-background")}><div className="mx-auto grid w-full max-w-6xl grid-cols-[minmax(0,1fr)_auto] items-center gap-4 px-5 py-4 sm:px-8"><div className="min-w-0"><Wordmark /></div><nav aria-label="Main" className="hidden items-center gap-8 lg:flex">{NAV_LINKS.map((link)=><Link key={link.to} to={link.to} className="link-underline text-sm font-medium text-foreground/80 hover:text-foreground" activeProps={{className:"text-primary"}}>{link.label}</Link>)}<WhatsAppButton size="sm" className="px-5">Talk to Megha</WhatsAppButton></nav><button type="button" onClick={()=>setOpen((v)=>!v)} aria-expanded={open} aria-controls="mobile-nav" aria-label={open?"Close menu":"Open menu"} className="flex h-11 w-11 shrink-0 items-center justify-center border border-ink/20 text-foreground lg:hidden">{open?<X className="h-5 w-5"/>:<Menu className="h-5 w-5"/>}</button></div><div id="mobile-nav" hidden={!open} className="rule-top surface-grain bg-background px-5 pb-10 pt-6 lg:hidden"><nav aria-label="Mobile" className="flex flex-col">{NAV_LINKS.map((link)=><Link key={link.to} to={link.to} className="border-b border-border py-4 font-display text-2xl text-foreground" activeProps={{className:"text-primary"}}>{link.label}</Link>)}</nav><p className="mt-6 text-xs tracking-widest text-muted-foreground">{TAGLINE}</p></div></header>; }
