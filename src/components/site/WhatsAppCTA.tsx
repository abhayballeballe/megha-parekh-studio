import { useRouterState } from "@tanstack/react-router";
import { MessageCircle } from "lucide-react";
import { whatsappLink, type WhatsAppIntent } from "@/lib/brand";
function intentForPath(pathname: string): WhatsAppIntent { if (pathname.startsWith("/tarot")) return "tarot"; if (pathname.startsWith("/vastu")) return "vastu"; return "general"; }
export function WhatsAppCTA() { const pathname = useRouterState({ select: (s) => s.location.pathname }); const href = whatsappLink(intentForPath(pathname)); return <><a href={href} target="_blank" rel="noopener noreferrer" className="fixed bottom-6 right-6 z-40 hidden items-center gap-2 border border-ink/20 bg-background/90 px-4 py-2.5 text-xs font-semibold tracking-wide text-foreground backdrop-blur-sm transition-colors duration-300 hover:border-oxblood hover:text-primary md:inline-flex"><MessageCircle className="h-4 w-4" aria-hidden="true" />Talk to Megha</a><div className="fixed inset-x-0 bottom-0 z-40 border-t border-border bg-background/95 backdrop-blur-md md:hidden"><div className="px-4 pt-3" style={{ paddingBottom: "max(0.75rem, env(safe-area-inset-bottom))" }}><a href={href} target="_blank" rel="noopener noreferrer" className="flex min-h-12 w-full items-center justify-center gap-2 bg-primary px-5 text-sm font-semibold text-primary-foreground"><MessageCircle className="h-4 w-4" aria-hidden="true" />Talk to Megha</a></div></div></>; }
